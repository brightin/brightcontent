<div class="page-header">
  <%= link_to "#{I18n.t 'brightcontent.create_new'} #{resource_class.model_name.human}", polymorphic_url([parent, resource_class], action: :new), class: "btn btn-primary pull-right" %>
  <h1><%= resource_class.model_name.human.pluralize %></h1>
</div>

<%= render_if_exists "index_top" %>

<div class="panel-body">
  <ol class="sortable_tree" data-rebuild_url="<%=rebuild_pages_url%>" data-max_levels="5">
    <%= sortable_tree @pages %>
  </ol>
</div>
